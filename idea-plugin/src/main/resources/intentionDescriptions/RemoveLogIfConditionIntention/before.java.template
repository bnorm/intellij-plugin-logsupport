public class X implements Runnable {

	private final static Logger log = LoggerFactory.getLogger(X.class);

	public void run() {
		<spot>if (log.isInfoEnabled())</spot>
			log.info("LOG001020:Starting to process X");
	}
}